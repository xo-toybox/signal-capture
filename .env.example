# Supabase — public (safe for client-side)
# A runtime guard blocks writes to remote Supabase in dev mode.
# Use `make dev-docker` for local Supabase, or `make db-pull` to seed local data.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Supabase — secret (server-only, never expose)
SUPABASE_SERVICE_ROLE_KEY=

# Auth — secret
ALLOWED_EMAIL=

# GitHub — optional (bug reporter creates GitHub issues when configured)
# The app works without these; the bug reporter just shows "not configured".
GITHUB_REPO=xo-toybox/signal-capture
# GitHub — bug reporter secret (optional)
#   https://github.com/settings/personal-access-tokens/new
#   Fine-grained PAT with Issues Read and Write
GITHUB_TOKEN=
